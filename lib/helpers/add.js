var backand = require('@backand/nodejs-sdk');

module.exports = function(functionName, envVars, callback){


	// read templates/module.yaml

	// fill module.json

	// write module.yaml

	callback(null, { data: {}});
}